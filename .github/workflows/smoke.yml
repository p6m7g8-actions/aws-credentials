name: Smoke

on:
  workflow_dispatch: {}

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: p6m7g8-actions/checkout@main

      - name: Validate wrapper wiring
        shell: bash
        run: |
          rg -n "aws-actions/configure-aws-credentials@v6.0.0" action.yml
